@PACKAGE_INIT@

# Check if target already available
if(NOT TARGET sph-z85::sph-z85)
    # provide path for scripts
    list(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_LIST_DIR}")
    include("${CMAKE_CURRENT_LIST_DIR}/sph-z85-targets.cmake")

